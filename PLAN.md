# URacquet.com V2 — Site Plan
*Sept 20, 2026 · Rebuild from scratch as a static site. The Wix site stays live and untouched until cutover.*

## 1. Goal
One fast, indexable, correct website for URacquet Shop that ranks for what Philadelphia tennis players actually search — **racquet stringing Philadelphia / near Penn / University City**, **tennis shop West Philadelphia**, **racquet demo Philadelphia**, **racquet customization** — and converts to a call, a visit, or a drop-off. Every page has one job and one keyword.

## 2. Why static, and why now
- The Wix site has been `noindex` on every page since launch (see `../uracquet-site-audit-2026-09-20.md`). Google has never seen it. A V2 that is *correct on day one* can be indexed within days of going live.
- Static HTML + CSS: no platform, no plugins, no monthly fee, sub-second loads, total control of titles/schema/markup — the three things Wix got wrong. Hosted free on Cloudflare Pages (or GitHub Pages). Mark never needs to log into anything; content edits are text-file edits.
- No JavaScript framework. A ~100-line Python build script assembles pages from a shared layout + per-page content, so header/footer/schema are written once.

## 3. Information architecture (URLs are final — they carry rankings later)
| URL | Page | Job | Target query | H1 (draft) |
|---|---|---|---|---|
| `/` | Home | Orient + convert (call / directions / services) | racquet stringing Philadelphia · tennis shop West Philadelphia | Master Racquet Stringing & Tennis Shop in West Philadelphia |
| `/services/` | Services | The money page: stringing, customization, grips, grommets, grip build-up, prices, rush | tennis racquet stringing Philadelphia · racquet customization | Tennis & Squash Racquet Stringing and Customization in Philadelphia |
| `/products/` | Products | Racquets, string, shoes, accessories; brands | tennis racquets Philadelphia · tennis string shop | Tennis & Squash Racquets, String, Shoes & Accessories |
| `/demo/` | Demo Program | How it works, pricing, rules | racquet demo program Philadelphia · try tennis racquets | Racquet Demo Program — Try 3 Racquets for 7 Days |
| `/about/` | About | Mark's credentials; trust | Mark Kuczynski master racquet technician · Penn stringer | Mark Kuczynski, USRSA Master Racquet Technician |
| `/visit/` | Visit | Address, hours, parking, map, contact | URacquet Shop hours · 4711 Pine Street | Visit URacquet Shop — 4711 Pine St, West Philadelphia |
| `/local/` | Philadelphia Tennis | Evergreen resources: Penn varsity, pro events, USTA/Flex leagues, public courts | Philadelphia tennis leagues · courts near University City | Tennis in Philadelphia: Leagues, Courts & Events |
| `/press/` | Press | Court Theory profile (link + short quote), WTA 125 role, Penn stringer, tour work | Mark Kuczynski stringer WTA Philadelphia | In the Press |
| `/404.html`, `/sitemap.xml`, `/robots.txt` | plumbing | | | |

Navigation: Home · Services · Products · Demo · About · Visit · (Local, Press in footer + secondary nav). Phone number in the header on every page, click-to-call on mobile. Sticky "Call · Directions" bar on mobile.

## 4. Per-page SEO plumbing (built into the layout, not remembered later)
- One `<title>` (≤60 chars) and `<meta name="description">` (≤155) per page — drafts in the copy deck.
- One H1 per page; H2s for sections; no decorative text in headings.
- Canonical URL per page; `robots: index,follow` (the opposite of the Wix site).
- JSON-LD sitewide: `SportingGoodsStore` (subtype of LocalBusiness) — name, address, geo, phone, email, hours (`openingHoursSpecification`), priceRange, image, `sameAs` (Instagram, University City District listing), `hasOfferCatalog` for the priced services. `FAQPage` on `/services/` and `/demo/`. `BreadcrumbList` on inner pages.
- Open Graph + Twitter card with one branded image (storefront/racket wall).
- Image alt text written by hand; images served as WebP ≤1600px with width/height attributes (no layout shift).
- `sitemap.xml` + `robots.txt` generated by the build.

## 5. Design direction (from the brand kit)
- **Palette from the logo and the shop itself:** logo green `#5DB13F`-ish and lime `#A6D653`-ish (sample exact values from `assets/source/brand_logo.png`), the dark green of the wordmark (`~#0F6B33`), white, near-black text. The shop's interior wall is lime green — the photos already carry the brand. Navy from the Wix nav bar is optional as a single accent; not required.
- **Type:** the wordmark is a heavy geometric sans (Verdana Bold-like) — use it as an image/SVG for the logo lock-up; body text in a system stack (`system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial`) — zero font downloads, instant render.
- **Tone:** confident, technical, friendly — the Court Theory profile's voice. Short paragraphs, prices visible, no stock photos: every image is ours.
- **Hero (home):** `wix_08` (stack of strung racquets) or `wix_06` (Mark at the machine) with the one-line promise and two buttons: *Call (215) 586-3649* · *Get Directions*.
- Layout: single column, max-width ~72ch for text, card rows for services/products, generous whitespace; mobile-first; dark-mode aware via `prefers-color-scheme` (light palette primary).

## 6. Asset inventory (`assets/source/`)
| File | Content | Use |
|---|---|---|
| wix_06.jpg | Mark stringing on the Wilson machine, lime wall | Home hero alt / Services hero / About |
| wix_13.jpg | Mark measuring a racquet on the Head balance machine | Services → Customization |
| wix_08.jpg | Stack of strung racquets (© Yeqiao Zhou) | Home hero / OG image candidate |
| wix_07.jpg, wix_14.jpg | Racket wall, bags (© Yeqiao Zhou) | Products |
| wix_05.jpg | String reels — Wilson, Head, Yonex (© Yeqiao Zhou) | Products → String / Services → Stringing |
| wix_11.jpg | Accessories collage: Head bag, Gamma dampeners, Tecnifibre, Penn balls, shoes | Products → Accessories |
| wix_09.jpg, wix_10.jpg | Customer browsing / swinging a demo racquet | Demo Program |
| wix_12.jpg | Penn's outdoor courts | Local |
| wix_04.jpg | Snowy courts with skyline | Local (seasonal) or About |
| wix_02.jpg, wix_03.jpg | Arthur Ashe Stadium; USTA National Championships banner | About / Press (tour credentials) |
| wix_01.png | UR circle logo (square) | favicon / app icon |
| brand_logo.png / _lime / _black_white | Logo lock-ups with address | Header, footer, print |
| brand_URacquet Shop Logo Lettering.pdf | Dark-green wordmark | Header wordmark (convert to SVG) |
| brand_tshirt_v2.jpg | "URACQUET SHOP · Philadelphia, PA · est. 2026" | Merch teaser (optional) |
| *(pending Dropbox sync)* logo.png, business_description.docx, U Raquet Shop Proposal.pdf, URacquet Shop Flyer.pdf, soft-opening PDFs, 20260818_wta_flier.pdf, Option_map.png, diagram_final.pdf | brand/copy/history | Fold into About, Press, Visit when they download |
| `../RacketDemoProgram/` | Your own demo-tracking app (Flask + Supabase PWA) | Internal tool — out of scope for the public site; future "demo availability" link possible |

Copyright: photos watermarked "© Yeqiao Zhou, URacquet" are the household's own. The Court Theory article is Allen McDuffee's — the Press page links and quotes briefly (≤2 sentences); it does not reproduce the article.

## 7. Content corrections carried into V2 (from the Wix copy)
1. "Knowledgable" → **Knowledgeable** (site-wide hero).
2. About: "relaxing with his fiancée and their three cats" → **"his wife and their cats"** (married 2026; the census is now four cats and a parrot — wording is the keeper's call).
3. Services: add **squash** where stringing is described (Products already says "tennis and squash").
4. Location: **canonical hours** — the site and the University City District listing disagree (site: Tue–Wed 12–6:30, Thu 1–6:30, Fri 12:30–6:30, Sat 11–6, Sun 12–5, Mon closed; UCD: Tue–Fri 12–7, Sat 11–7, Sun 11–5). Decide once; V2, Google Business Profile, UCD and Instagram all get the same table.
5. Home: the dated "Local Tennis News" list (last item April 2026) becomes an evergreen `/local/` resources page — nothing on the site should carry a date that can go stale.
6. Add the press and the WTA 125 story — currently absent from the site entirely.
7. Footer social: Instagram only (@uracquetshop); drop placeholder Facebook/Twitter icons unless real accounts exist.

## 8. Technical spec
- **Structure:** `content/*.md` (copy deck, human-edited) → `build.py` → `site/` (deployable). Templates in `templates/layout.html`. No dependencies beyond Python 3.
- **CSS:** one file, custom properties for the palette, mobile-first, ~300 lines. **JS:** ~20 lines for the mobile menu; nothing else.
- **Forms:** phone (`tel:`) and email (`mailto:`) links first; optional contact form via Formspree (free tier) — decision below.
- **Map:** Google Maps embed (place query, no API key) on `/visit/` + a "Get Directions" link.
- **Hosting:** Cloudflare Pages (free; custom domain; automatic HTTPS; deploy by drag-and-drop or `git push`). Analytics: Cloudflare Web Analytics (free, cookieless) or none.
- **Domain strategy (two options — keeper's ruling):**
  - **A. Temporary URL first:** V2 goes live at `uracquet-v2.pages.dev` (indexable), linked from Instagram bio and the Google Business Profile; Wix untouched; cut `uracquet.com` over when satisfied.
  - **B. Cut over on completion:** point `uracquet.com` DNS at Pages the day V2 is approved. Wix remains intact (not edited, just no longer served) and can be re-pointed in minutes. Faster to rank; the domain is the asset.
- **Off-site (platform-independent, highest leverage):** claim/verify the Google Business Profile; NAP + hours identical everywhere; Yelp listing; ask Court Theory for a link.

## 9. Open questions (answer in any order; none blocks the build)
1. Canonical hours (item 7.4).
2. Where is `uracquet.com` registered (Wix or elsewhere)? Determines the DNS step.
3. Contact form, or phone/email only? (Form = Formspree account under uracquet@gmail.com.)
4. About wording: "wife and their cats" — include the parrot?
5. Press page: OK to quote two sentences of the Court Theory piece with a link? (Yes by default.)
6. Anything from the AlcarazCamp/WTA flier/soft-opening materials that belongs on the site (an events section? a photo?).
7. Squash stringing: offered? (Products implies yes.)

## 10. Build sequence
1. **Today:** this plan + the copy deck (`content/`) — review the copy; mark corrections.
2. **Build (one session):** `build.py`, layout, CSS, 8 pages, schema, sitemap, optimized images, alt text. Preview locally in the browser pane.
3. **Review with Mark** on his phone and the shop laptop; fix copy.
4. **Deploy** to Cloudflare Pages (temporary URL). Search Console property; submit sitemap.
5. **Off-site:** Google Business Profile; hours sync; Yelp.
6. **Cutover** per option A/B.

## 11. Build status — Sept 20, 2026
- **Built.** `python3 build.py` → `site/` (8 pages + 404, sitemap.xml, robots.txt, `_headers`, favicon set, OG image, 14 photos as WebP at 800/1600 px with hand-written alt text, LocalBusiness + FAQ + Breadcrumb JSON-LD). 4.4 MB total including images.
- **Verified locally** (browser pane, `python3 -m http.server 8765 --directory site`): desktop and 375-px mobile layouts, dark mode, mobile menu, sticky call bar, Maps embed, no console errors, server log clean (all 200/304).
- **Fixes during build:** image paths made absolute (`/img/…`) so inner pages resolve; favicon.ico generated from the logo mark.
- **Press URL confirmed:** https://court-theory.beehiiv.com/p/thank-the-stringer-mark-kuczynski-uracquet-philadelphia
- **To preview later:** from `Pets/`, run `python3 -m http.server 8765 --directory mark/uracquet-v2/site` and open http://localhost:8765 (or use the desktop app's preview config `uracquet-v2`).
- **To deploy (Cloudflare Pages, free):** dash.cloudflare.com → Workers & Pages → Create → Pages → *Upload assets* → drag the `site/` folder → project name `uracquet-v2` → gets `uracquet-v2.pages.dev`. For the temporary URL, rebuild first with `BASE_URL=https://uracquet-v2.pages.dev python3 build.py` so canonicals and the sitemap point at the live host; at domain cutover, rebuild with the default BASE_URL and add the custom domain in Pages.
- **Still open (PLAN §9):** canonical hours · registrar · contact form or not · About wording · squash/turnaround FAQ answers · anything from the pending Dropbox files.

## 12. Hosting on AWS (keeper has an account) — two routes, both fine
- **Route 1 — Amplify Hosting (recommended, no CLI needed):** AWS Console → Amplify → *Host web app* → *Deploy without Git* → drag the `site/` folder (or a zip of its contents) → app deploys to `https://<branch>.<id>.amplifyapp.com` with HTTPS. Clean URLs (`/services/`) work natively; `customHttp.yml` in `site/` sets the cache/security headers; add a rewrite rule `/<*>` → `/404.html` (404) in *Rewrites and redirects*. Custom domain: Amplify → Domain management → add `uracquet.com` (Route 53 or any DNS; certificate is automatic). Cost: free tier covers this site many times over.
- **Route 2 — S3 + CloudFront (classic):** private S3 bucket + CloudFront distribution with Origin Access Control; ACM certificate in us-east-1; the small CloudFront Function `cloudfront-index-rewrite.js` (viewer-request) so `/services/` serves `index.html`; custom error response 404 → `/404.html`. Deploy with `./deploy-s3.sh` (set `BUCKET`, `DIST_ID`). Cost: ~$0.50/month or less.
- Either way: rebuild with `BASE_URL=<live host>` before the first upload so canonicals/sitemap match; at cutover rebuild with the default and point DNS.

## 13. Hosting on GitHub Pages (keeper has GitHub) — the simplest route of all
- **No AWS, no Cloudflare needed.** GitHub Pages serves static sites free, with automatic HTTPS and custom domains. Fits this site exactly.
- **Setup (once):** create a GitHub repo (public — Pages on a free account requires it; the site content is public anyway), push this folder, then repo *Settings → Pages → Source: GitHub Actions*. The included workflow `.github/workflows/pages.yml` publishes `site/` on every push to `main`. Site appears at `https://<user>.github.io/<repo>/` within a minute.
- **Custom domain (at cutover):** Settings → Pages → Custom domain `www.uracquet.com` (GitHub writes a `CNAME` file into the artifact — for this workflow, add `CNAME` containing `www.uracquet.com` to `site/` via build.py instead); at the registrar add a CNAME `www → <user>.github.io` and four A records for the apex (185.199.108–111.153); tick *Enforce HTTPS*. Rebuild with the default BASE_URL. The Wix site is untouched until DNS moves.
- **Workflow:** edit `content/`/`build.py` → `python3 build.py` → commit `site/` → `git push` → live. Originals in `assets/source/` stay in Dropbox (git-ignored by size); the built site and brand files are committed.
- **Gotcha handled:** `.nojekyll` is written into `site/` so GitHub doesn't run Jekyll (which would drop underscore files like `_headers`).
- Temporary-URL note: on the `<user>.github.io/<repo>/` URL the site lives under a sub-path; links here are root-absolute (`/img/…`), so **use a repo named `<user>.github.io`** (served at the root) or attach the custom domain right away — both avoid sub-path breakage. Recommended: attach `www.uracquet.com` at first deploy, since it takes minutes to reverse.

## 14. LIVE — Sept 20, 2026
- **V2 is published at https://yeqiaoz.github.io/uracquet/** via GitHub Pages (repo github.com/yeqiaoz/uracquet, workflow deploys `site/` on every push to `main`; deploy key `~/.ssh/github_uracquet`, repo-scoped, write). All pages, CSS, images, sitemap return 200.
- The Wix site at uracquet.com is untouched and still noindexed; the temporary URL is indexable now. **Do not submit the temporary URL to Search Console as the shop's site** — wait for the domain, or you'll be ranking a github.io address.
- **Next:** (1) canonical hours ruling → update `HOURS` in build.py; (2) Mark's review of copy on his phone; (3) domain cutover: rebuild with default `BASE_URL`, write `CNAME` (`www.uracquet.com`) into `site/`, add the custom domain in Settings → Pages, set DNS at the registrar (CNAME www → yeqiaoz.github.io; apex A records 185.199.108.153 / .109.153 / .110.153 / .111.153), enforce HTTPS; (4) Google Business Profile + Search Console on the real domain.
- **Editing workflow from here:** change `content/`-driven text in `build.py` (or ask), run `python3 build.py`, commit, push. ~1 minute to live.

## 15. Search visibility — Sept 20, 2026
- **Done (no account needed):** robots.txt allows all; sitemap.xml lists 8 URLs; every page `index, follow` + self-canonical; **IndexNow submitted (202)** → Bing/DuckDuckGo/Yandex. Key file `indexnow-key.txt` (persisted; `build.py` publishes it as `site/<key>.txt`). Re-submit after content changes: same POST with the changed URLs.
- **Correction to §14's caution:** indexing the temporary github.io URL is fine after all — once a custom domain is attached, GitHub Pages 301-redirects `yeqiaoz.github.io/uracquet/*` to it, so any ranking carries over. Submit the temp URL to Search Console now; move the property to the real domain at cutover.
- **Google — two steps only the owner can do:** (1) Search Console → Add property → *URL prefix* `https://yeqiaoz.github.io/uracquet/` → verify by **HTML tag**: paste the `content="…"` value into `gsc-token.txt` in this folder (or send it), rebuild/push, click Verify. Then *Sitemaps* → submit `sitemap.xml`, and *URL inspection* → Request indexing on the home page. (2) Google Business Profile: claim/verify the shop, set the website field to the live URL (later the real domain) — the Maps pack is where "stringing near me" is decided.
- **The real lever is the domain:** uracquet.com → GitHub Pages. See the DNS block in the chat log / §13.
